<UserControl x:Class="PosClient.Desktop.Features.Orders.Creator.CustomerSearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:PosClient.Desktop.Features.Orders.Creator"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:OrderCreatorViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <!-- The Search Box -->
        <ui:TextBox x:Name="SearchBox"
                    PlaceholderText="Search customer (name, phone or email)"
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Icon="{ui:SymbolIcon Search24}"
                    ClearButtonEnabled="True"
                    GotFocus="SearchBox_GotFocus"
                    LostFocus="SearchBox_LostFocus"
                    TextChanged="SearchBox_TextChanged"
                    PreviewKeyDown="SearchBox_PreviewKeyDown"
                    PreviewMouseDown="SearchBox_PreviewMouseDown"/>

        <!-- The Dropdown Popup -->
        <Popup x:Name="ResultsPopup"
               PlacementTarget="{Binding ElementName=SearchBox}"
               Placement="Bottom"
               VerticalOffset="5"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Slide">

            <Border Background="{DynamicResource ApplicationBackgroundBrush}"
                    BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="5"
                    Width="{Binding ActualWidth, ElementName=SearchBox}"
                    MaxHeight="250">

                <Grid>
                    <!-- 3. The Results List -->
                    <ListView x:Name="ResultsList"
                              Background="Transparent"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              ItemsSource="{Binding SearchResults}"
                              SelectedItem="{Binding SelectedCustomer}"
                              ScrollViewer.ScrollChanged="ResultsList_ScrollChanged"
                              SelectionChanged="ResultsList_SelectionChanged"
                              BorderThickness="0">

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <ui:SymbolIcon Symbol="Person24"
                                                   FontSize="20"
                                                   Margin="0,0,10,0"
                                                   Foreground="{DynamicResource SystemAccentColorBrush}"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding PhoneNumber}" FontSize="11" Opacity="0.7"/>
                                        <TextBlock Text="{Binding Address}" FontSize="11" Opacity="0.5" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    
                                </Grid>
                                
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        
                    </ListView>

                    <!-- No Results Message -->
                    <TextBlock Text="No customers found."
                               Visibility="{Binding ShowNoResults, Converter={StaticResource BoolToVisConverter}}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Opacity="0.6"
                               Margin="10,50"
                               IsHitTestVisible="False"/>

                    <!-- Loading Indicator -->
                    <ui:ProgressRing IsIndeterminate="True"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                                     Width="30" Height="30"
                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                     Margin="0,50"
                                     IsHitTestVisible="False"/>

                </Grid>
                
            </Border>
            
        </Popup>

    </Grid>
</UserControl>
