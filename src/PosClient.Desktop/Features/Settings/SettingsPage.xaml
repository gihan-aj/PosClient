<Page x:Class="PosClient.Desktop.Features.Settings.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PosClient.Desktop.Features.Settings"
      xmlns:helpers="clr-namespace:PosClient.Desktop.Features.Settings.Converters"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      d:DataContext="{d:DesignInstance local:SettingsPage,
                                 IsDesignTimeCreatable=False}"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Page.Resources>
        <helpers:ApplicationThemeToBooleanConverter x:Key="ThemeToBooleanConverter"/>
    </Page.Resources>

    <StackPanel Margin="20">
        <TextBlock
            FontSize="20"
            FontWeight="Medium"
            Text="Personalization" />
        <TextBlock Margin="0,12,0,0" Text="Theme" />
        <RadioButton
            Margin="0,12,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_light"
            Content="Light"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource ThemeToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}" />
        <RadioButton
            Margin="0,8,0,0"
            Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
            CommandParameter="theme_dark"
            Content="Dark"
            GroupName="themeSelect"
            IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource ThemeToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}" />

        <TextBlock
            Margin="0,24,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="Sequences" />
        <ItemsControl ItemsSource="{Binding ViewModel.Sequences, Mode=OneWay}" Margin="0,12,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Id}" FontWeight="SemiBold" />
                                <TextBlock Text="Sequence ID" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock x:Name="PrefixText" Text="{Binding Prefix}" />
                                <TextBox x:Name="PrefixEdit" Text="{Binding Prefix, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed" Margin="0,0,8,0" />
                                <TextBlock Text="Prefix" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock x:Name="ValueText" Text="{Binding CurrentValue}" />
                                <ui:NumberBox x:Name="ValueEdit" Value="{Binding CurrentValue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Visibility="Collapsed" Margin="0,0,8,0" SpinButtonPlacementMode="Inline" />
                                <TextBlock Text="Current Value" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                <TextBlock Text="{Binding PreviewNext}" />
                                <TextBlock Text="Next Preview" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:Button x:Name="EditBtn" Icon="{ui:SymbolIcon Edit24}" Appearance="Transparent"
                                           Command="{Binding DataContext.ViewModel.EditSequenceCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                           CommandParameter="{Binding}" />
                                
                                <ui:Button x:Name="SaveBtn" Icon="{ui:SymbolIcon Checkmark24}" Appearance="Success" Visibility="Collapsed"
                                           Command="{Binding DataContext.ViewModel.SaveSequenceCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                           CommandParameter="{Binding}" Margin="0,0,4,0"/>
                                
                                <ui:Button x:Name="CancelBtn" Icon="{ui:SymbolIcon Dismiss24}" Appearance="Secondary" Visibility="Collapsed"
                                           Command="{Binding DataContext.ViewModel.CancelEditSequenceCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                           CommandParameter="{Binding}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                            <Setter TargetName="PrefixText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PrefixEdit" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ValueText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ValueEdit" Property="Visibility" Value="Visible" />
                            <Setter TargetName="EditBtn" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="SaveBtn" Property="Visibility" Value="Visible" />
                            <Setter TargetName="CancelBtn" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock
            Margin="0,24,0,0"
            FontSize="20"
            FontWeight="Medium"
            Text="About" />
            <TextBlock Margin="0,12,0,0" Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />
    </StackPanel>
</Page>
