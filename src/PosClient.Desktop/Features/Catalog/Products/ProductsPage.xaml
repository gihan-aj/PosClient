<Page x:Class="PosClient.Desktop.Features.Catalog.Products.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PosClient.Desktop.Features.Catalog.Products"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      d:DataContext="{d:DesignInstance local:ProductsViewModel, IsDesignTimeCreatable=False}"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20, 20, 20, 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Products" FontSize="24" FontWeight="SemiBold"/>

            <ui:Button Grid.Column="1"
                       Content="New Product"
                       Icon="{ui:SymbolIcon Add24}"
                       Appearance="Primary"
                       Command="{Binding NavigateToAddCommand}"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Text="Category" IsEditable="True" Width="150" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" Text="Brand" IsEditable="True" Width="150" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="2" Text="Status" IsEditable="True" Width="150" Margin="0,0,10,0">
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="Inactive"/>
            </ComboBox>
            <ui:TextBox Grid.Column="3"
                        PlaceholderText="Search products..."
                        Icon="{ui:SymbolIcon Search24}"
                        Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <DataGrid Grid.Row="2" Margin="20,0,20,20"
                     ItemsSource="{Binding ViewModel.Products}"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     HeadersVisibility="Column"
                     SelectionMode="Single"
                     BorderThickness="1"
                     BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Image" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="50" Height="50" CornerRadius="4"
                                    Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                <ui:SymbolIcon Symbol="Image24" Foreground="Gray" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Product" Width="3*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel VerticalAlignment="Center" Margin="10">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding CategoryName}" Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text=" • " Foreground="Gray"/>
                                    <TextBlock Text="{Binding Sku}" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Price" Binding="{Binding BasePrice, StringFormat='{}{0:C}'}" Width="Auto" FontWeight="Medium"/>

                <DataGridTemplateColumn Header="Stock" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding TotalStock}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding VariantCount, StringFormat='{}{0} variants'}" 
                                           FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
                                <Border Background="{Binding StockStatusBar}" 
                                        Height="4" CornerRadius="2" Width="40" Margin="0,4,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Status" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="#E6F4EA" CornerRadius="4" Padding="8,4" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Active" Foreground="#1E8E3E" FontWeight="SemiBold" FontSize="12"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button Icon="{ui:SymbolIcon MoreVertical24}" 
                                       Appearance="Transparent"
                                       ToolTip="Actions"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
            
        </DataGrid>
    </Grid>
</Page>
