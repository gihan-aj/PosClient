<ui:ContentDialog x:Class="PosClient.Desktop.Features.Catalog.Products.Editor.Tabs.QuickVariantGeneratorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:PosClient.Desktop.Features.Catalog.Products.Editor.Tabs"
             mc:Ignorable="d"
             Title="Quick Add Variants"
             PrimaryButtonText="Create Variants"
             SecondaryButtonText="Cancel"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">

            <TextBlock Text="Select Sizes:" FontWeight="SemiBold"/>
            <WrapPanel>
                <CheckBox x:Name="ChkSizeS" Content="S" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkSizeM" Content="M" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkSizeL" Content="L" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkSizeXL" Content="XL" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkSizeXXL" Content="XXL" Margin="0,0,8,8"/>
            </WrapPanel>

            <TextBlock Text="Select Colors:" FontWeight="SemiBold" Margin="0,6,0,0"/>
            <WrapPanel>
                <CheckBox x:Name="ChkColorBlue" Content="Blue" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkColorRed" Content="Red" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkColorBlack" Content="Black" Margin="0,0,8,8" IsChecked="True"/>
                <CheckBox x:Name="ChkColorWhite" Content="White" Margin="0,0,8,8"/>
            </WrapPanel>

            <TextBlock Text="{Binding InfoText, RelativeSource={RelativeSource AncestorType=ui:ContentDialog}}" Foreground="Gray"/>

            <TextBlock Text="Initial Stock Per Variant" FontWeight="SemiBold" Margin="0,6,0,0"/>
            <ui:NumberBox x:Name="NumberInitialStock" Value="10" Minimum="0" Maximum="100000" Width="120"/>

            <TextBlock Text="Price (uses base price)" FontWeight="SemiBold" Margin="0,6,0,0"/>
            <StackPanel Orientation="Vertical">
                <RadioButton x:Name="RbUseBasePrice" Content="Same as base" IsChecked="True" Margin="0,2,0,0"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="RbCustomPrice" Content="Custom per variant" Margin="0,2,8,0"/>
                    <ui:NumberBox x:Name="NumberCustomPrice" Width="120" Minimum="0" Maximum="100000" IsEnabled="False"/>
                </StackPanel>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</ui:ContentDialog>
